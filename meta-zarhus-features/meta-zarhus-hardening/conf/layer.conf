# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "zarhus-hardening"
BBFILE_PATTERN_zarhus-hardening = "^${LAYERDIR}/"
BBFILE_PRIORITY_zarhus-hardening = "6"

# This should only be incremented on significant changes that will
# cause compatibility issues with other layers
LAYERVERSION_zarhus-hardening = "1"

LAYERDEPENDS_zarhus-hardening = " \
    core \
    openembedded-layer \
    "

LAYERSERIES_COMPAT_zarhus-hardening = "scarthgap master"
