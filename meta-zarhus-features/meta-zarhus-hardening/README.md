# Zarhus hardening layer

Layer for system hardening implementing, among others:

* Disable root account
* Add default user and force password change at first boot
* Enable and force Linux kernel lockdown and module signature verification
* Disable unneeded kernel drivers to lower attack surface
